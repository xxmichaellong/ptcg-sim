# V1 file-by-file audit

- Status: **historical summary; superseded by the 2026-09-23 review**.
  The [new findings](reviews/2026-09-23-v1-transfer-review.md) and
  [145 individual file mappings](reviews/2026-09-23-v1-file-inventory.md)
  account for the full frozen client/server tree. **File coverage is complete;
  parity is not.** Historical SAME/REFACTORED claims below must be read with
  the open findings in that review.
- Purpose: answer, for every file under `client/` and `server/`, what it did
  and where that behaviour lives in v2 (replicated, refactored, improved, or
  deliberately dropped), so nothing v1 users rely on is lost by accident.
- Method: each file was read; the behaviour it owns was checked against the
  v2 code and, where visible, measured on the live v1 site next to the v2
  preview at the same viewport (see `04-client-renderer-and-parity.md`).

Status legend: **SAME** identical visible behaviour; **REFACTORED** same
behaviour, different mechanism; **IMPROVED** behaviour kept, a v1 defect
removed (row in `PARITY_EXCEPTIONS.md`); **DROPPED** intentionally not
carried (ADR/exception cited); **OPEN** a difference still awaiting a product
decision.

## `client/src/actions/general`

| File                 | What it did                                                                                                                     | v2                                                                                                                                                                                                                                                                                                                                                                                  | Status   |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `board-actions.js`   | Discard / to hand / shuffle to deck / lost zone every loose card on the playboard, narrating "moved N card(s) from board to …". | `ResolveLooseBoardCards` (`game-core/decide-command.ts`), narrated from `LooseBoardCardsResolved`; context menu "Playboard" entries and Enter / Alt+Enter / `/` shortcuts.                                                                                                                                                                                                          | SAME     |
| `change-type.js`     | Reclassify a card in play as Energy / Tool / Pokémon so attach/evolve treat it accordingly.                                     | `ChangeCardCategory`; "Change type..." submenu and Alt+E/T/P.                                                                                                                                                                                                                                                                                                                       | SAME     |
| `close-popups.js`    | Escape / outside click: deselect, close full view, hide empty zone popups, hide context menu.                                   | `DismissLocalPresentation` scopes in `BoardSessionController`; overlays own their outside-dismiss. Work-area popups stay open while they hold cards, as v1's `hideZoneElementsIfEmpty` did.                                                                                                                                                                                         | SAME     |
| `count.js`           | Write deck/discard/lost-zone/hand counts into the frame labels.                                                                 | `ZoneCountSceneNode` projected by the scene at v1's anchors and colours.                                                                                                                                                                                                                                                                                                            | SAME     |
| `flip-board.js`      | Rotate the two frames, swap resizer/button colours, swap self-/opp-text classes, in solo swap the "self view" for the menus.    | `flipBoardLayoutState`; the route reports the perspective so the sidebar buttons and resizers swap colours and the Sort label re-uprights. In Solo (or a room where both players enabled board flip) the flipped board acts for the seat at the bottom: sidebar buttons, unselected keybinds, V, and the own-only menu entries all target it, and the wire commands name that seat. | SAME     |
| `flip-coin.js`       | Random heads/tails narrated as "X flipped heads".                                                                               | `FlipCoin` command; the authority rolls so both players see one result.                                                                                                                                                                                                                                                                                                             | IMPROVED |
| `reset.js`           | Clear every zone, counter and once-per-game marker for a player, rebuild the deck, narrate "reset" / "has an invalid deck!".    | `ResetPlayer`; identical narration.                                                                                                                                                                                                                                                                                                                                                 | SAME     |
| `reveal-and-hide.js` | Look/cover (private), reveal/hide (public) for prizes, hands and single cards; random face-down play; narration for each.       | `BeginZoneInspection`/`EndPrivateInspection`, `SetPublicReveal`, `SetZonePublicReveal`, `SetCardFace`, `PlayRandomCardFaceDown`; v1 wording in `PresentationEffects.ts`.                                                                                                                                                                                                            | SAME     |
| `rotate-card.js`     | Rotate a stack (R) or one card as BREAK (Alt+R), keep counters attached, bench margins for turned containers.                   | `RotateStack` / `SetCardOrientation`; `layoutLegacyPlayRow` reproduces the 2%/3% margins and markers follow the painted box.                                                                                                                                                                                                                                                        | SAME     |
| `setup.js`           | Reset, shuffle, draw seven, set six prizes, narrate.                                                                            | `SetupPlayer`.                                                                                                                                                                                                                                                                                                                                                                      | SAME     |
| `take-turn.js`       | Discard both boards, reset ability markers, reveal face-down in-play cards, draw one, "Turn N" / "drew for turn" / empty deck.  | `StartTurn` in `decideTableAction`.                                                                                                                                                                                                                                                                                                                                                 | SAME     |
| `undo.js`            | Solo-only undo by replaying the local action log minus the last action.                                                         | `ApplySoloUndo` with authoritative checkpoints (PX-010). Same button and `u` key; no "Loading..." label flash.                                                                                                                                                                                                                                                                      | IMPROVED |
| `VSTAR-GX.js`        | Toggle the per-player VSTAR/GX markers with "used their VSTAR!" / "reset their GX".                                             | `SetOncePerGameMarker`; frame buttons in `LegacyBoardChrome`.                                                                                                                                                                                                                                                                                                                       | SAME     |

## `client/src/actions/zones`, `counters`, `move-card-bundle`, `keybinds`

| File                                            | What it did                                                                                                                                                                                                                  | v2                                                                                                                                                                                                                                                                                                         | Status     |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| `zones/deck-actions.js`                         | Draw N (prompt), view top/bottom N (prompt) into the "Looking at cards..." popup, move to deck top/bottom, switch with top, shuffle in, to board.                                                                            | `DrawCards`, `ExtractDeckCardsForInspection`, `MoveCardToDeckTop/Bottom`, `SwapCardWithDeckTop`, `ShuffleCardIntoDeck`; the native prompts and their wording are kept (`CountPrompt`).                                                                                                                     | SAME       |
| `zones/general.js`                              | Popup bulk buttons (Discard all / Shuffle all / Shuffle to bottom / Lost Zone all / To Hand / Leave in play), Close, and the Sort checkbox that orders a zone's display by decklist order.                                   | `ResolveInspectionCards`, `ResolveStagedCards`, `RestoreStagedStack`, `ShuffleZone*`; `WorkAreaPanel` and `ZoneBrowser` overlays. Sort is paint-only as in v1 but orders by card name rather than decklist position (duplicates still group).                                                              | OPEN       |
| `zones/hand-actions.js`                         | Discard hand / shuffle hand to deck / to bottom, each drawing N after a prompt.                                                                                                                                              | `DiscardHandAndDraw`, `ShuffleHandIntoDeckAndDraw`, `ShuffleHandToDeckBottomAndDraw`.                                                                                                                                                                                                                      | SAME       |
| `zones/prizes-actions.js`, `shuffle-zone.js`    | Shuffle prizes / prizes to deck bottom; shuffle any zone with shared indices so both clients agree.                                                                                                                          | `MovePrizesToDeckBottom`, `ShuffleZone`; the authority shuffles once.                                                                                                                                                                                                                                      | IMPROVED   |
| `counters/damage-counter.js`                    | Editable red circle at `left + w/1.5, top + h/4`, size `w/3`; digits add/subtract tens; 0 removes.                                                                                                                           | `SetDamage`; `layoutLegacyActiveQ0Markers` from the painted box for every stack; the same digit shortcuts and inline editor.                                                                                                                                                                               | SAME       |
| `counters/special-condition.js`                 | Editable circle at the card's left with the P/B/A/Pa/C palette; `y` cycles.                                                                                                                                                  | `SetSpecialCondition`; same palette and cycle.                                                                                                                                                                                                                                                             | SAME       |
| `counters/ability-counter.js`, `use-ability.js` | Translucent tab across the card's middle; `w` toggles; narrated "used X's ability".                                                                                                                                          | `SetAbilityUsed` / `SetCardAbilityUsed`; legacy tab appearance.                                                                                                                                                                                                                                            | SAME       |
| `counters/reset-counters.js`                    | Clear every ability tab on attack / pass / turn.                                                                                                                                                                             | `AbilityMarkersReset` inside the table actions.                                                                                                                                                                                                                                                            | SAME       |
| `move-card-bundle/move-card.js` and siblings    | The whole card-movement engine: attach vs evolve vs plain move, container creation, energy/tool layering, evolution layers, counter transfer on evolve, auto active/bench swaps, stadium bump, cover updates, sort, refresh. | `game-core` commands (`MoveCard`, `MoveCardToPlay`, `MovePlayStack`, `PlaceCardOnPlayStack`, `MoveCardFromStack`, `MoveCardToStadium`, …) with the same outcomes: swaps, stadium bump, damage transfer on evolve, attachments staged into "Move attached cards". Geometry: `layoutLegacyPlayRow` (PX-005). | REFACTORED |
| `move-card-bundle/move-card-message.js`         | Narration grammar for moves ("moved / attached / evolved / shuffled … from … to …", hidden-zone "card").                                                                                                                     | `room-authority/narration-events.ts` with the same grammar and disclosure rules.                                                                                                                                                                                                                           | SAME       |
| `keybinds/keybinds.js`, `keybindSleep.js`       | Every shortcut, its modifiers, what it needs selected, replay/spectator gating; Shift shows the reference.                                                                                                                   | `resolveLegacyBoardShortcutAction.ts` + `LegacyBoardKeyboardShortcuts.tsx`; pinned by the `legacy-runtime-*-shortcuts` browser oracles against the real v1 runtime.                                                                                                                                        | SAME       |

## `client/src/setup`

| File                                                                                     | What it did                                                                                                                                                                                         | v2                                                                                                                                                                                                          | Status     |
| ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| `chatbox/append-message.js`                                                              | Append a `<p>` styled by type (player/announcement/message) to the solo or room log and mirror it to the room.                                                                                      | Activity feed (`LegacyPresentationSurface`) with the same classes; room chat via the authority.                                                                                                             | SAME       |
| `chatbox/export-chat.js`                                                                 | "1: message\n\n2: …" text download; clear log.                                                                                                                                                      | `serializeBattleLog`, same format.                                                                                                                                                                          | SAME       |
| `chatbox/hide-options-context-menu.js`                                                   | Options menu closes on outside mousedown.                                                                                                                                                           | `useDismissibleRoomOptions`.                                                                                                                                                                                | SAME       |
| `deck-builder/core/*`                                                                    | Native deck builder: card search, sort, compare, CSV import/export, deck state, validation.                                                                                                         | `apps/web/src/features/deck/*` (`deck-builder-store`, `tcgdex-card-catalog`, CSV io), gated by `legacy-deck-builder-browser-parity.spec.ts`.                                                                | REFACTORED |
| `deck-constructor/import.js`, `find-type.js`, `find-old-type*.js`, `sample.decklists.js` | Paste-import of PTCGL/Limitless lists, language and Pocket-set handling, card-type lookup, the 📖 sample decklists and 🪄 random deck, the editable decklist table (Confirm/Cancel/Save).           | `pasted-decklist-import.ts`, `limitless-decklist-*`, `popular-decklists*`, `LegacyDeckImportPanel.tsx` with the same controls.                                                                              | REFACTORED |
| `deck-constructor/build-deck.js`, `card.js`, `cover.js`, `exchange-data.js`              | Build `Card`/`Cover` image objects with their listeners; send the deck and card back to the opponent on join.                                                                                       | `LoadDeck` command; cards are projected scene nodes; card backs travel in the projection (`SetCardBack`).                                                                                                   | REFACTORED |
| `general/accept-action.js`, `process-action.js`, `add/clean-action-data.js`              | The v1 sync core: apply an action locally and emit it, keep per-player action logs with counters.                                                                                                   | Replaced by the authoritative room: commands → `decideCommand` → events → per-recipient projections. Centralizes command ordering and reconciliation; does not prove absence of synchronization defects.    | IMPROVED   |
| `general/catch-up-actions.js`, `resync-actions.js`, `spectator/*`                        | Periodic sync checks, catch-up on reconnect, spectator polling for the action log.                                                                                                                  | Reconnect resumes from the authority's revision; spectators receive projections directly.                                                                                                                   | IMPROVED   |
| `general/determine-deckdata.js`, `determine-username.js`                                 | Pick the right deck/name for self/opp in solo vs room; solo names are Blue/Red.                                                                                                                     | Solo rooms are created as Blue vs Red; room names come from the seat.                                                                                                                                       | SAME       |
| `general/pop-up-message.js`                                                              | Own-hand reveal confirmation dialog.                                                                                                                                                                | Own-hand reveal confirmation in `RemoteSessionBoard.tsx`.                                                                                                                                                   | SAME       |
| `general/preload-image.js`                                                               | Warm the card-back image at load.                                                                                                                                                                   | Card back served from `/v2/assets/cardback.png` and painted by the scene; no separate preload needed.                                                                                                       | REFACTORED |
| `general/replay-block.js`                                                                | Which keybinds/menu entries stay usable in replay (look/reveal only).                                                                                                                               | Replay disclosure entries in `selectLegacyContextEntries`; shortcuts fail closed in replay (PX-001).                                                                                                        | SAME       |
| `general/shuffle.js`                                                                     | Fisher–Yates index lists shared over the wire.                                                                                                                                                      | Authority-side shuffles.                                                                                                                                                                                    | IMPROVED   |
| `home-header/header-toggle.js`                                                           | Tab switching (Solo/Multiplayer/Deck/Settings), closing the changelog/donations/deck workspace, replay tab label.                                                                                   | `RemoteRoomRoute` / `RemoteRoomLobby` tab state; `ReplayModeShell` relabels the tab "Replay".                                                                                                               | SAME       |
| `image-logic/click-events.js`                                                            | Select on click, full view on double click, context menu with per-zone entry visibility and the menu positioning rules; cover click opens the pile view and narrates "is looking through X's deck". | `BoardSurface` intents, `selectLegacyContextEntries`, `ContextMenu` placement, `ZoneBrowser`; the deck-view narration is kept.                                                                              | SAME       |
| `image-logic/drag.js`                                                                    | HTML5 drag: drop on a stack card attaches/evolves, on the deck cover goes to top, on a zone moves; the target zone tints and a target card rings while hovering.                                    | Pointer drag (`BoardDragController`) with `resolveBoardDropTarget`; the DOM renderer tints the target zone and rings the target stack's top card; drops are optimistic. Self/no-op drops rejected (PX-002). | IMPROVED   |
| `image-logic/remove-images.js`, `reset-image.js`                                         | DOM housekeeping for card images.                                                                                                                                                                   | Not needed: the scene is rebuilt from state.                                                                                                                                                                | REFACTORED |
| `settings/settings.js`                                                                   | Dark mode class toggling across both frames and every panel; hide containers; hide opponent hand (solo); change background (prompt: URL / blank / theme).                                           | `BoardPreferences` + route classes; `requestBrowserRoomBackground` with the same prompt text and theme images; default wallpaper differs (review F-07).                                                     | OPEN       |
| `sizing/resizer.js`, `adjust-alignment.js`, `refresh-board.js`                           | Drag handles between the frames, rescale attached-card offsets, re-append containers to repair the DOM after every move, hand justify-content when overflowing.                                     | Resize handles in `LegacyBoardChrome` + `resizeBoardLayoutState`; layout is a pure projection (PX-005). An overflowing hand keeps full-size cards and scrolls (`ZoneSceneNode.scroll`, `handScrollPx`).     | SAME       |
| `zones/get-zone.js`                                                                      | Zone registry per user with arrays and DOM elements.                                                                                                                                                | `MatchState.zones` / projections.                                                                                                                                                                           | REFACTORED |

## `client/src/initialization`

| File                                                                                   | What it did                                                                                                                                                             | v2                                                                                                                                                                                  | Status     |
| -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| `global-variables/global-variables.js`                                                 | Socket, frame documents, `systemState` (mode, names, decks, card backs, turn), `mouseClick` selection, wallpaper + `-200px` offset.                                     | Session/controller state; `styles.css` default wallpaper and offset.                                                                                                                | REFACTORED |
| `document-event-listeners/card-context-menu/*`                                         | Wire every context-menu entry to its action.                                                                                                                            | `resolveLegacyBoardOverlayAction.ts`.                                                                                                                                               | SAME       |
| `document-event-listeners/sidebox/header-buttons.js`                                   | Tabs; leaving a room from the Solo tab with a confirm.                                                                                                                  | Route tabs; Leave Room confirm text kept.                                                                                                                                           | SAME       |
| `document-event-listeners/sidebox/import-deck.js`, `native-deck-builder*.js`           | Deck tab: P1/P2 targets, Import, Confirm/Cancel/Save, 📖, 🪄, card back, language, native builder workspace.                                                            | `features/deck/*`.                                                                                                                                                                  | REFACTORED |
| `document-event-listeners/sidebox/p1/bottom-buttons.js`                                | Set Up / Reset / Both; Options menu: replay in/out with the four replay stepping buttons, import state, export state (file or share URL), export/clear log, fullscreen. | `RemoteRoomLiveControls` + `LegacyReplayControls`; export state downloads a perspective replay. "Import game state" and the share-URL export are deferred (ADR-021, PX-007/PX-008). | DROPPED    |
| `document-event-listeners/sidebox/p1/chat-buttons.js`, `p2/chat-buttons.js`            | Attack / Pass (discard board + narrate), Undo, 🌺 as an unnamed player line, Enter sends chat.                                                                          | `DeclareAttack` / `PassTurn`; the 🌺 line is unnamed.                                                                                                                               | SAME       |
| `document-event-listeners/sidebox/p1/changelog.js`, `donations-link.js`, `tutorial.js` | In-page changelog and donations pages, tutorial video modal.                                                                                                            | `LegacyWelcome.tsx`.                                                                                                                                                                | SAME       |
| `document-event-listeners/sidebox/p2/room-buttons.js`                                  | Generate / copy a room id, join (random trainer name if blank), spectator and coaching checkboxes, leave room.                                                          | `RemoteRoomLobby`: rooms are created by the server and joined by invitation (ADR-020); one copy button; the random trainer names are the same list; header reads `id: CODE`.        | REFACTORED |
| `document-event-listeners/sidebox/settings.js`                                         | Settings checkboxes and background button.                                                                                                                              | `RemoteRoomSettings.tsx`.                                                                                                                                                           | SAME       |
| `document-event-listeners/table/board-buttons.js`                                      | Turn, coin, flip, refresh images, fullscreen playmat (hides the sidebar), VSTAR/GX.                                                                                     | `LegacyBoardChrome` controls; fullscreen playmat toggles `playmatExpanded`.                                                                                                         | SAME       |
| `document-event-listeners/table/zone-buttons.js`, `zones.js`, `resizer.js`             | Popup buttons and zone listeners; resizer mouse handlers.                                                                                                               | Overlays and `ReactDomBoardResizeInteraction`.                                                                                                                                      | SAME       |
| `document-event-listeners/table/documents.js`, `window/window.js`                      | Global click/contextmenu/keydown/keyup on all three documents; resize → refresh; `beforeunload` warning.                                                                | Route-level handlers; resize via `ResizeObserver`. The generic `beforeunload` prompt is only kept for an unsaved deck-builder draft.                                                | OPEN       |
| `mutation-observers/hand-observer.js`, `prizes-observer.js`                            | Re-justify the hand when it overflows; shrink prize images past six.                                                                                                    | Scene layout handles both counts (prize grid grows rows).                                                                                                                           | OPEN       |
| `load-import-data/load-import-data.js`                                                 | Apply a `/import?key=` share-link state embedded by the server at page load.                                                                                            | Deferred (ADR-021).                                                                                                                                                                 | DROPPED    |
| `socket-event-listeners/socket-event-listeners.js`                                     | Every inbound room event: join/reject/reconnect/leave announcements, chat, the mirrored action calls, export-state results.                                             | `RemoteGameSession` over the authority WebSocket; presence and chat narrated with the same wording; "Room is full" surfaces as a header failure instead of an overlay.              | REFACTORED |

## Markup, styles, assets, server

| File                                                                    | What it did                                                                                                                                                                                                                  | v2                                                                                                                                                                                       | Status   |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `client/index.ejs`                                                      | Page shell: two frame iframes, stadium, resizers, board buttons, tabs, Solo/Multiplayer/Deck/Settings panels, context menu, options menu, keybind modal, changelog, donations, tutorial, decklist table, title/meta/favicon. | `apps/web/index.html` + React route; every id and label carried; keybind modal, changelog (`legacy-changelog.html`), donations and tutorial reproduced.                                  | SAME     |
| `client/self-containers.html`, `opp-containers.html`                    | Zone markup per frame: centre circles, counts, covers, popups with their buttons, hand, sort checkbox, VSTAR/GX.                                                                                                             | Scene zones/counts + `LegacyBoardChrome`; centre circles painted by the DOM renderer's frame nodes.                                                                                      | OPEN     |
| `client/src/css/index.css`, `self-containers.css`, `opp-containers.css` | All geometry (percent regions, card sizing, margins), colours, dark mode variants, popup panels.                                                                                                                             | `renderer-contract/layout.ts` (ratios pinned by `legacy-viewport-generalization.spec.ts`), `styles.css`, overlay CSS; measured against v1 at 1280x720 / 1440x900 / 1600x900 / 1920x1080. | OPEN     |
| `client/src/assets/*`                                                   | Card back, blank logo, favicon, theme backgrounds.                                                                                                                                                                           | `apps/web/public/v2/assets/*`; all five original assets are now bundled byte-for-byte (review F-06).                                                                                     | SAME     |
| `server/server.js`                                                      | Express + Socket.IO rooms (2 players + spectators, reconnect grace), SQLite key/value store for share links, `/import?key=`, an admin password.                                                                              | Cloudflare Worker + Durable Object rooms with capabilities and invitations, continuation saves (ADR-012), no default admin credential (PX-009), share links deferred (ADR-021).          | IMPROVED |

## Second pass (2026-09-18): the large files read in full

The first pass covered every file; this pass re-read the ones that were only
sampled -- `index.ejs` (1,023 lines), both frame documents, `index.css`
(1,844 lines), `import.js`, `sample.decklists.js`, `native-deck-builder.js`,
`socket-event-listeners.js`, `reveal-and-hide.js`, `resizer.js` -- element by
element. Five gaps were found and closed:

| Gap                                                                                                                 | v2 now                                                                                       |
| ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `index.ejs` warns a phone or tablet on load that the sim expects a desktop.                                         | `announceMobileNotice` in `apps/web/src/mobile-notice.ts`, same user-agent list and wording. |
| The Sponsors & Donations page lists four sponsor tiers; v2 stopped after the two links.                             | The tier lists are back in `LegacyWelcome`, styled like v1's inline `<ul>`.                  |
| The upper frame paints its popup headers in the red seat colour (`opp-containers.css`), not the lower frame's blue. | The work-area popup header follows its side.                                                 |
| The deck-import review's Save writes `decklist.csv`; only the builder's Export writes `ptcg-sim-deck.csv`.          | Both filenames are carried.                                                                  |
| Revealing a card from your own hand in a room opens "Press OK to stop revealing card to opponent" (`showPopup`).    | The same modal, in v1's `custom-popup` markup and styling; OK submits the hide.              |

Two v1 behaviours were deliberately not replicated, each for a reason:

- **`keybindSleep.js`** freezes keybinds for 350 ms whenever a remote action
  arrives, because v1 addresses cards by array index and a board that changed
  underneath could make a keybind act on the wrong card. v2 addresses cards by
  stable id and every command carries expectations that fail closed, so the
  freeze would only add input latency.
- **"Loading spectator view..."** marks v1's spectator catching up on the
  action log. v2 spectators receive a projection; there is no catch-up phase.

Dropping a card into an open work-area popup (v1 lists `viewCards` and
`attachedCards` as drop targets) was the third; it was implemented afterwards
as `MoveCardToWorkArea` and PX-013 is closed. A host dragged in takes its
stack with it and stages the dependents, as v1's `relocateAttachedCards` does;
the only refusal left is v2's one-window rule for the attached-card popup,
which already governs every other stack departure.

## Open items found by this audit

Both items the audit left open (the flipped Solo board acting for the other
seat; the scrolling hand) were carried the same day. Two minor differences
remain:

- ~~The zone Sort checkbox orders by card name, where v1 ordered by decklist
  position.~~ Carried: the projection discloses a display-only decklist rank
  for cards the viewer may read, and both sorts paint v1's declared order.
- On a flipped board the ephemeral declarations (chat prefix, "mulligans",
  "is looking through … deck") still carry the connected seat's name; the
  seat-bound commands (draw, coin, setup, turn, hand actions, undo) narrate
  the seat they act for.
