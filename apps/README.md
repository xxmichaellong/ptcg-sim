# v2 applications

Application entry points live here. The React client and room-runtime adapter are
added only after their renderer and authority contracts have executable tests;
the legacy `client/` and `server/` remain independently deployable throughout the
strangler migration.
